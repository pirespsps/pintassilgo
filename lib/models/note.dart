class Note {
  int? id;
  String title;
  String? text;
  int idImage;
  String? anexo;
  String? link;

  Note({required this.title, this.text, required this.idImage, this.anexo, this.link, this.id}); 
}